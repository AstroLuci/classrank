# Turso (required on Render so data survives sleep/redeploy)
TURSO_DATABASE_URL=libsql://your-db-name-your-org.turso.io
TURSO_AUTH_TOKEN=your-turso-token

# Optional: only needed for local file storage override; unused when Turso URL is set
# DATA_DIR=data
